def recommend_destination(user_request: str) -> str:
    # pretend "research"
    return "Tokyo — great food, safe, lots of city exploration, and strong public transit."

def search_flights(destination: str) -> str:
    # pretend "flight search"
    return f"Sample flights to {destination}: (A) Budget airline 1-stop, (B) Full-service 1-stop, (C) Best time-of-day option."

def search_hotels(destination: str) -> str:
    # pretend "hotel search"
    return f"Sample hotels in {destination}: (1) Budget clean hotel, (2) Mid-range near transit, (3) Nice stay central area."